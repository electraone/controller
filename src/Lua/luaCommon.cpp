#include "luaCommon.h"

extern bool lua_windowRepaintEnabled;
/*
void repaintPageGraphics (Component *component)
{
	if (App::get()->getAppWindows ()->getState () == AppUiState::page)
	{
		if (lua_windowRepaintEnabled)
		{
			component->repaint ();
		}
	}
}
*/
